import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF2D4FD3);     // Blue used in intro
  static const Color light = Color(0xFFF4F6FD);       // Light backgrounds
  static const Color accent = Color(0xFFFFC107);      // Gold or orange
  static const Color darkText = Color(0xFF222222);    // Headlines
  static const Color lightText = Color(0xFF777777);   // Descriptions
}
